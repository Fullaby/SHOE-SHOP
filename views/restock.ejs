<%- include('partials/header', {title : 'Restock'}) %> 
    <main>
        <div class="container col-6 mt-5">
          <h1 class="text-center">Restock Shoe</h1>
          <% if(errors){ %>
            <div class="p-2 rounded-1" style="background-color:rgb(255, 205, 214);">
                <p style="color:rgb(255, 53, 53); ">* <%= errors %> </p>
            </div>
          <% } %> 

          <form actions="/admin/add" method="post">
            
            <div class="mb-2">
              <label class="form-label">Amount</label>
              <input type="number" class="form-control" id="stock" name="stock">
            </div>

            <button type="submit" class="btn btn-secondary">Add</button>
            <a href="/admin" type="submit" class="btn btn-danger">Cancel</a>
          </form>

        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>